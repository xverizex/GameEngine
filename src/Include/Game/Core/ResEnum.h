#ifndef RES_ENUM_H
#define RES_ENUM_H

/*
 * This file generate automaticaly with builder_data tool
 */

enum {
	R_SHADERS,
	R_FONTS,
	R_TEXTURES,
	N_R
};

enum {
	RES_RECT_FRAG,
	RES_RECT_VERT,
	RES_SPRITE_FRAG,
	RES_SPRITE_VERT,
	RES_SPRITE_WITH_CAMERA_FRAG,
	RES_SPRITE_WITH_CAMERA_VERT,
	RES_TEXT_FRAG,
	RES_TEXT_VERT,
	N_SHADER_RES
};

enum {
	RES_TERMINUS_TTF,
	N_FONTS_RES
};

enum {
	N_TEXTURES_RES
};

#endif
